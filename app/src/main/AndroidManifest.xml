<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartblindassistant">

    <!-- Permissions (MUST be outside <application>) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Optional: Require camera hardware -->
    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartBlindAssistant">

        <!-- Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartBlindAssistant">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OCR Reader Activity -->
        <activity
            android:name=".CameraReaderActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartBlindAssistant" />

        <!-- Object Detection Activity -->
        <activity
            android:name=".ObjectAvoidanceActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartBlindAssistant" />
        <activity android:name=".NavigationAssistanceActivity"  />


        <activity android:name=".CurrencyRecognitionActivity"
            android:exported="true" />

    </application>

</manifest>
